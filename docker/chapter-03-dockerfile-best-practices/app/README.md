# App Code (Chapter 03)

This directory contains the sample Python application used in Chapter 03.

The application is intentionally small and focused on demonstrating Dockerfile caching and dependency management.

- `main.py` — the application entrypoint
- `requirements.txt` — dependencies installed in the Dockerfiles
- `run.sh` — optional helper to run the app locally (outside of Docker)
