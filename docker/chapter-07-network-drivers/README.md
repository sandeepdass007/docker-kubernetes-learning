# Chapter 07 — Network Drivers

This chapter explores Docker’s built-in network drivers and their behavior.

It includes examples for:

- **bridge** — the default Docker network driver (with both bad and good examples)
- **host** — shares the host network stack
- **none** — no networking at all

Each folder contains a self-contained example and a README for that driver.
