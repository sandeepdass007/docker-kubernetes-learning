# Bridge Network Driver (Chapter 07)

This folder contains examples showing how Docker’s default bridge network behaves.

- `bad/` demonstrates what happens when you rely on default networking without creating a user-defined network.
- `good/` demonstrates the correct way to use a user-defined bridge network so that containers can resolve each other by name.

Each subfolder has its own `README.md` with detailed instructions.
