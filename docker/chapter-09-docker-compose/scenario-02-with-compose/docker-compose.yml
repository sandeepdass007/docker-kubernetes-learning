version: "3.9"

services:
  server:
    build: ./server
    container_name: compose-server
    ports:
      - "5001:5001"

  client:
    build: ./client
    container_name: compose-client
    depends_on:
      - server